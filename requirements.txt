# Dipendenze runtime (installate sulla VPS: pip install -r requirements.txt).
# La logica core usa solo la libreria standard; queste servono a funzioni specifiche.
feedparser>=6.0          # parsing dei feed RSS (brief)
edge-tts>=6.1            # TTS di fallback gratuito (podcast)
internetarchive>=3.5     # upload audio su archive.org (podcast)
